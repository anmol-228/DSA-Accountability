name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.12', '3.14']

    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - uses: actions/setup-node@v7
        with:
          node-version: '24'

      - name: Install Python dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Public portability and secret sanity
        shell: pwsh
        run: .\scripts\check-public-safety.ps1

      - name: Python tests
        shell: pwsh
        env:
          QT_QPA_PLATFORM: offscreen
          GIT_AUTHOR_NAME: DSA Accountability CI
          GIT_AUTHOR_EMAIL: ci@dsa-accountability.invalid
          GIT_COMMITTER_NAME: DSA Accountability CI
          GIT_COMMITTER_EMAIL: ci@dsa-accountability.invalid
        run: python -m pytest tests -q

      - name: Chrome extension tests
        shell: pwsh
        run: node --test tests_js\*.test.js
